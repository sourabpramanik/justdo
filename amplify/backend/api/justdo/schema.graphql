type TodoItem @model @auth(rules: [{allow: owner}]) {
  id: ID!
  content: String!
  userID: ID @index(name: "byUser")
}

type User @model @auth(rules: [{allow: owner}]) {
  id: ID!
  name: String!
  TodoItems: [TodoItem] @hasMany(indexName: "byUser", fields: ["id"])
}
